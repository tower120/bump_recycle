[package]
name = "recycled_bump"
version = "0.1.0"
edition = "2024"

[features]
allocator_api = []

[dependencies]

[dev-dependencies]
itertools = "0.15"
criterion = "0.8.2"
bumpalo = {version = "=3.20.3", features=["allocator_api"]}

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[[bench]]
name = "alloc"
harness = false

[[bench]]
name = "vec"
harness = false